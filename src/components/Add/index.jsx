import React from "react";
import "./add.scss";

const Add = () => {
  return (
    <div className="add">
      <button type="button" className="addButton">
        Add
      </button>
    </div>
  );
};

export default Add;
